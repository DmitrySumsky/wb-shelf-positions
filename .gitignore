# Снапшоты пересобираются каждым прогоном
snapshot*.json
matrix.csv
*.log
*.txt
!requirements.txt

# Ключи сервисных аккаунтов — только через секреты репозитория
*credentials*.json
*service*account*.json
dds-ganza-*.json

__pycache__/
*.pyc
.venv/

# Карта «бренд → scriptId» для кнопки в книгах (чужие проекты, не код)
apps_script/deployed.json
